// Setup token requirement removed — setup wizard is open on fresh installs.
// The only protection against re-running setup on an already-installed instance
// is the `installed` flag check in the setup route itself.
